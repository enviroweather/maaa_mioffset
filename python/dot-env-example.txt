# mioffset .env environment settings
# copy this example to a file named .env and complete
# do not put share or put into maaa_offset git repository
# only use these in a file named .env when using with that project
# values from April 2026

# AWS configu
USER_NAME=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
REGION_NAME=
BUCKET_NAME=

# local file locations, set base dir first
BASE_DIR=
NARR_INPUT_DIR=HF5_DIR="${BASEDIR}/h5"
NARR_INPUT="${HF5_DIR}/narr_latlon.h5"
OUTPUT_OFFSET_DIR="${BASE_DIR}/MAAA_MIOFFSET/tmp"

# parameters
TIME_FLAG="F"



