# mioffset .env environment settings
# copy this example to a file named .env and complete
# do not put share or put into maaa_offset git repository
# only use these in a file named .env when using with that project
# values from April 2026 
# note AWS_SESSION_TOKEN is left blank and it works

# this value is still being tested
# where to find data for this session: on disk ="file" or AWS/S3 = "S3"
NARR_DATA_LOCATION="S3"


# AWS config
USER_NAME=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
REGION_NAME=
AWS_SESSION_TOKEN=
NARR_BUCKET=


# local file locations, set base dir first
# this is optional shortcut, can put full paths in each value below
BASE_DIR=

# if NARR timeseries hdf5 files are being use, path to those
NARR_DATA_DIR="${BASEDIR}/h5"

# if NARR timeseries JSON files are used, path to those
NARR_JSON_DIR="${BASE_DIR}/json"

# if NARR grid index HDF5 file is on disk, path to that
NARR_GRID_LATLON="${NARR_DATA_DIR}/narr_latlon.h5"

# if NARR grid index HDF5 file, filename.  
#   the program assumes same bucket as other data files
NARR_GRID_LATLON_S3="narr_latlon.h5"

# where to save output files (PNG etc)
OUTPUT_OFFSET_DIR="${BASE_DIR}/MAAA_MIOFFSET/tmp"

# where to write logs
MIOFFSET_LOG_DIR="../logs"


# default parameters
TIME_FLAG="F"


