[pytest]
testpaths = tests
# adds fod/ to sys.path so tests can import narr_data, aws, etc. directly
pythonpath = fod
markers =
    integration: marks tests that require external resources (NARR files or AWS/S3)
