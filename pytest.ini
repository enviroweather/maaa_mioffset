[pytest]
testpaths = tests
pythonpath = src
markers =
    integration: marks tests that require external resources (NARR files or AWS/S3)
    s3: marks tests that require live AWS S3 access (subset of integration)
